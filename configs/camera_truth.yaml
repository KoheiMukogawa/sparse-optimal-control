# カメラ真値計測の設定（設計: specs/2026-07-16-camera-truth-pipeline-design.md）
# K / dist は rover/calibrate_camera.py の出力を貼り付ける。
# floor_tags はタグ中心の実測コース座標 [m]（原点=走行原点, x=第1直進方向）。
# タグの印刷サイズ精度は不要（中心と光線交点のみ使用。サイズは検出可否にだけ効く）。
camera:
  name: c270_720p           # 使用カメラ・モードを記録（レンズ切替時は要再キャリブ）
  image_size: [1280, 720]   # キャリブ動画の解像度・向き。本番動画と不一致なら即エラー
  # 2026-07-16 キャリブ（WIN_20260716_08_42_18_Pro.mp4・再投影誤差0.261px・
  # 画面全域カバー・ブレ除去後62枚）
  K:
    - [1374.49, 0.00, 724.61]
    - [0.00, 1377.31, 383.39]
    - [0.00, 0.00, 1.00]
  dist: [0.16015, -0.30996, -0.00027, 0.01458, 0.00000]
floor_tags:      # id: [x_m, y_m]。4枚とも実測して記入
  # 2026-07-16 実測（1m L字コース）。id1 はユーザー指定の (40,40)
  # （距離実測からの推定は(44,38.6)、画像からの推定は(49,44)＝不一致未解決。
  # 暫定±5cm扱い、静置照合で要確定）
  0: [0.00, -0.20]
  1: [0.40, 0.40]
  2: [1.20, 1.16]
  3: [1.20, -0.30]
robot_tag:
  id: 10
  z_m: 0.12            # 床→タグ面の高さ（実測して更新。0.12は照合用の箱の値）
  yaw_offset_rad: 0.0  # タグ正準+x と機体前方のズレ
